#include <stdio.h>
/**
 * main - prints the alphabet in lowercase
 * Return: 0 (success)
 */
int main(void)
{
	char x = 'a';

	while (x != '{')
	{  putchar(x);
		x++;
	}
	putchar('\n');
	return (0);
}
